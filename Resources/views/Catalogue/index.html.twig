{% extends 'PumukitWebTVBundle::layout.html.twig' %}

{% block body %}
<p class="main_menu_text">This service shows the currently available catalogs of audiovisual files. These files are divided between the public and private media libraries (depending on whether or not they are publicly available to the general public), and grouped according to the selected criteria.</p>
  <h1 class="title-for-crumbs">{% trans %}Repository Catalogue{% endtrans %}</h1>
  {% set lastChar = '--' %}
    {% for tag in provider_tags %}
      {% if tag.title|slice(0,1)|upper != lastChar %}
        <h3>{{tag.title|slice(0,1)|upper}}</h3>
      {% endif %}
      {% include 'PumukitGeantWebTVBundle:Catalogue:serieslike_tag.html.twig' with {'tag': tag } %}
      {% set lastChar = tag.title|slice(0,1)|upper %}
    {% endfor %}
{% endblock %}
